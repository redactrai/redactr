# Caddy reverse proxy for redactr-server.
# Caddy automatically provisions and renews a TLS certificate for the domain
# via ACME (Let's Encrypt) on port 443. REDACTR_DOMAIN is read from the
# environment (set in .env / docker-compose).
{$REDACTR_DOMAIN} {
	reverse_proxy redactr-server:8080
}
